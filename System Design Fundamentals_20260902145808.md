# System Design Fundamentals



## Chat Room

- **Chat Room ID**: chat-20260902-d5c89c58-88d2-49c2-8b33-0cf465fb4ef8

- **User ID**: h6a61fTs5mp_CKcBwQOgixSulC9fPrgw_k3h1iaaXk0

- **Model**: GPT-5.6 (通常版、思考時間：なし / Thinking Level：None)

- **Plugin**: chinese



---

**User** _(2026-09-02 11:36:46)_

如何做系统设计，请结合方法论以及一个复杂的例子说明，并说明系统设计应考虑哪些问题，需要具备哪些知识体系

---

**Bot** _(2026-09-02 11:38:33)_

系统设计的核心不是“画出一张架构图”，而是在**明确目标与约束**的前提下，系统性地做出一系列取舍：性能、可用性、一致性、成本、复杂度、研发效率与安全性之间往往不能同时最优。

一个好的系统设计方案应能回答：

- 系统要解决什么问题？边界在哪里？
- 规模多大？流量、数据量、增长速度如何？
- 为什么采用这个架构，而不是其他方案？
- 高峰、故障、攻击、数据不一致时会怎样？
- 如何上线、监控、扩容、回滚和演进？

---

# 一、系统设计的方法论

可以将系统设计过程概括为以下十步。实际工作或面试中不一定严格按顺序，但这个框架能避免遗漏关键问题。

## 1. 澄清需求：先定义问题，而非急于选技术

需求通常分为两类。

### 功能性需求（Functional Requirements）

即系统“要做什么”。

例如一个电商秒杀系统可能需要：

- 用户查看商品和库存；
- 用户在活动开始后下单抢购；
- 防止超卖；
- 创建订单并完成支付；
- 查询订单状态；
- 支持取消未支付订单并恢复库存；
- 支持运营配置活动、商品、库存和限购规则。

### 非功能性需求（Non-functional Requirements）

即系统“要做到什么程度”。

常见维度包括：

| 维度 | 需要澄清的问题 |
|---|---|
| 性能 | 请求延迟要求是多少？P95/P99 延迟目标是多少？ |
| 吞吐量 | 峰值 QPS、写入量、消息量是多少？ |
| 可用性 | 是否要求 99.9%、99.99% 或更高？ |
| 一致性 | 库存、余额、订单是否允许短暂不一致？ |
| 扩展性 | 用户和数据增长十倍后如何扩容？ |
| 安全性 | 如何认证、授权、防刷、防篡改、防泄漏？ |
| 成本 | 是否能接受多机房、强一致数据库等高成本方案？ |
| 可维护性 | 如何监控、排障、灰度发布和版本回滚？ |

> 设计系统前必须先确认“最重要的目标”。  
> 例如，支付系统通常优先保证正确性与可追溯性；内容推荐系统通常更重视吞吐量和低延迟；日志系统则更强调写入能力和成本。

---

## 2. 明确范围与优先级

复杂系统不可能一次性解决所有问题。应当把需求划分为：

- **必须实现（Must Have）**
- **应该实现（Should Have）**
- **可以后续实现（Could Have）**
- **明确不做（Out of Scope）**

例如，设计第一版秒杀系统时：

- 必须：限购、库存扣减、订单创建、支付超时释放库存；
- 应该：防刷、排队、监控告警；
- 后续：跨地域多活、个性化推荐、复杂优惠组合；
- 不做：完整的售后、物流、评价体系。

这样可以避免设计过度：还没有百万 QPS，就引入跨地域多活、复杂服务网格和十几种中间件，往往只会增加故障面。

---

## 3. 容量估算：用数量驱动架构

系统设计必须有基本的量化估算，否则很难判断单体、缓存、分库分表、消息队列等方案是否必要。

常见估算指标：

- 日活用户（DAU）；
- 峰值并发用户数；
- 平均 QPS 与峰值 QPS；
- 读写比例；
- 单条数据大小；
- 每日新增数据量；
- 数据保存周期；
- 网络带宽；
- 可接受的响应时间。

### 常用公式

```text
平均 QPS = 每日请求总量 / 86,400

峰值 QPS ≈ 平均 QPS × 峰值系数

每日存储增量 = 每日新增记录数 × 单条记录平均大小

N 年存储量 = 每日存储增量 × 365 × N
```

例如：

- 一场秒杀活动有 1,000 万用户访问；
- 活动高峰集中在前 10 秒；
- 每个用户平均发起 3 次请求；
- 则入口峰值可能达到：

```text
1,000 万 × 3 / 10 = 300 万 QPS
```

这意味着不能让所有请求直接穿透到数据库；必须在 CDN、网关、缓存、限流和异步队列多个层面削峰。

---

## 4. 从核心数据模型开始

数据模型决定了系统的一致性边界、查询方式与扩展难度。

设计时需回答：

- 核心实体有哪些？
- 它们之间的关系是什么？
- 哪些字段必须强一致？
- 哪些字段允许异步更新？
- 查询主要按什么维度进行？
- 是否需要分库、分表或冷热分层？
- 是否需要审计记录、历史版本或软删除？

例如订单系统常见实体：

```text
用户 User
商品 Product
库存 Inventory
秒杀活动 FlashSaleCampaign
秒杀资格/令牌 PurchaseToken
订单 Order
订单明细 OrderItem
支付 Payment
退款 Refund
```

其中：

- `库存` 与 `订单创建` 密切相关；
- `支付` 是外部系统交互，必须支持幂等；
- `订单状态` 是一个状态机；
- 操作日志与事件记录应可追溯。

---

## 5. 画出高层架构：先分层，再分服务

初版架构不必过早拆得很细，但应该明确职责边界。

一个典型互联网系统可分为：

```text
客户端
  ↓
CDN / WAF / DNS / 负载均衡
  ↓
API 网关
  ↓
业务服务层
  ├── 用户服务
  ├── 商品服务
  ├── 订单服务
  ├── 库存服务
  ├── 支付服务
  └── 风控服务
  ↓
基础设施层
  ├── 缓存（Redis）
  ├── 消息队列（Kafka / RocketMQ 等）
  ├── 关系型数据库
  ├── 搜索引擎
  ├── 对象存储
  └── 监控、日志、链路追踪
```

划分服务时遵循几个原则：

1. **按业务领域划分，而不是按数据库表划分。**
2. **服务要有清晰的数据归属。**
3. **高频读写、不同扩缩容特征的模块可独立拆分。**
4. **避免服务之间形成大量同步链式调用。**
5. **先模块化，规模和组织结构成熟后再拆微服务。**

---

## 6. 识别关键路径与瓶颈

不是所有接口都同等重要。系统设计应优先深入最关键、最危险的路径。

例如秒杀系统中：

- 商品详情页：读多写少，可高度缓存；
- 抢购请求：极端高并发，必须限流和削峰；
- 库存扣减：正确性敏感，不能超卖；
- 创建订单：涉及事务和幂等；
- 支付回调：可能重复、乱序、延迟到达；
- 订单查询：读多写少，适合读模型或缓存。

系统设计的重点通常是：

> 将“海量无效流量”挡在外层，将“有限的有效请求”有序地送到真正需要强一致处理的核心链路。

---

## 7. 明确一致性策略与事务边界

分布式系统无法简单地把单机数据库事务扩展到所有服务。必须明确哪些地方需要强一致，哪些地方可以最终一致。

### 常见一致性层次

| 场景 | 通常要求 |
|---|---|
| 支付金额、账户余额 | 强一致、可审计 |
| 商品库存 | 通常要求不超卖；少卖是否可接受需视业务决定 |
| 订单状态展示 | 可接受秒级最终一致 |
| 商品浏览量、点赞数 | 通常可最终一致 |
| 搜索索引 | 一般最终一致 |
| 推荐结果 | 最终一致或离线更新即可 |

### 常见解决方式

- 本地数据库事务；
- 乐观锁（版本号 / CAS）；
- 悲观锁；
- 分布式锁；
- 消息队列异步处理；
- Outbox（事务发件箱）模式；
- Saga（长事务补偿）模式；
- 幂等键与去重表；
- 状态机；
- 对账与补偿任务。

> 不要将“分布式事务”作为默认答案。  
> 很多业务更适合“本地事务 + 可靠事件 + 幂等消费 + 对账补偿”的最终一致性方案。

---

## 8. 设计异常、故障和降级路径

优秀设计不仅描述“正常时怎么运行”，更要说明“出错时如何不失控”。

需要考虑：

- 缓存失效、缓存穿透、击穿、雪崩；
- 消息重复、积压、丢失、乱序；
- 数据库主从延迟、故障切换；
- 下游服务超时；
- 网络分区；
- 重试风暴；
- 热点数据与热键；
- 用户重复点击；
- 支付回调重复；
- 恶意请求、爬虫、DDoS；
- 配置错误、灰度发布异常；
- 机房故障。

常见保护手段：

```text
超时 + 限流 + 熔断 + 降级 + 重试（带退避）+ 隔离 + 幂等 + 补偿 + 监控告警
```

特别注意：**重试不是免费的**。若下游已过载，无限制重试会形成重试风暴，使故障扩大。

---

## 9. 设计可观测性与运营能力

如果无法发现问题、定位问题、恢复问题，系统即使架构再漂亮也不可运营。

至少应具备：

### 指标（Metrics）

- QPS、成功率、错误率；
- P50、P95、P99 延迟；
- CPU、内存、磁盘、网络；
- 缓存命中率；
- 数据库连接池、慢查询、复制延迟；
- 消息堆积量、消费延迟；
- 库存扣减失败率；
- 下单成功率、支付成功率。

### 日志（Logs）

- 结构化日志；
- 请求 ID、用户 ID、订单号、活动 ID；
- 敏感字段脱敏；
- 错误日志可检索、可关联。

### 链路追踪（Tracing）

为每个请求分配 Trace ID，追踪其经过的网关、服务、缓存、数据库和消息队列，便于定位慢调用和错误来源。

### 告警与预案

告警应基于业务影响，而不仅是机器指标。例如：

- 下单成功率低于阈值；
- 消息积压持续增长；
- 库存扣减异常陡增；
- 支付回调延迟异常；
- 数据库主从延迟超过安全值。

---

## 10. 说明取舍、演进路径与验证方案

系统设计没有“唯一正确答案”，关键在于取舍是否合理。

例如：

- 使用缓存可以降低数据库压力，但会引入缓存一致性问题；
- 使用异步消息可削峰和解耦，但会带来最终一致性和重复消费；
- 分库分表提升写入扩展能力，但增加跨分片查询和运维复杂度；
- 多活提升容灾能力，但成本、冲突处理和数据复制更复杂。

设计完成时最好补充：

1. **为什么这样选；**
2. **不用什么方案，以及原因；**
3. **系统规模增长后如何演进；**
4. **如何通过压测、故障演练和对账验证正确性。**

---

# 二、复杂案例：高并发电商秒杀系统设计

下面以“限量商品秒杀”为例，完整说明如何将方法论落地。

---

## 1. 业务背景与目标

假设平台在某场活动中销售一款限量商品：

- 库存：10 万件；
- 活动开始前有 1,000 万用户访问；
- 大量请求会集中在前 10 秒；
- 每位用户限购 1 件；
- 下单后 15 分钟内未支付，自动取消订单并释放库存；
- 核心目标：**不超卖、系统不崩、用户体验可接受、订单可追溯。**

---

## 2. 关键需求

### 功能需求

- 展示活动商品与倒计时；
- 活动开始后用户可请求抢购；
- 每人限购一件；
- 成功后创建待支付订单；
- 接收支付平台异步回调；
- 未支付订单超时取消；
- 释放相应库存；
- 用户查询订单状态。

### 非功能需求

| 项目 | 目标示例 |
|---|---|
| 峰值入口流量 | 数百万 QPS |
| 可售库存 | 10 万件 |
| 库存正确性 | 不超卖 |
| 抢购接口延迟 | 尽可能低，允许排队或异步返回 |
| 订单创建 | 可最终一致，但不能重复创建 |
| 支付结果 | 必须幂等且可对账 |
| 可用性 | 活动期间核心服务需高可用 |
| 安全 | 防刷、防重放、防伪造请求 |

---

## 3. 最主要的挑战

### 挑战一：瞬时流量远大于实际库存

1,000 万用户访问、10 万库存，意味着绝大多数请求必然失败。

若每个请求都打到数据库：

```text
数百万 QPS → 数据库连接耗尽 → 全站雪崩
```

因此，要让无效请求尽可能在 CDN、网关和缓存层被拦截。

### 挑战二：库存不能超卖

多个请求同时判断“还有库存”，再分别扣减，会产生并发竞争。

错误示例：

```text
读取库存 = 1
请求 A 判断库存 > 0
请求 B 判断库存 > 0
A 扣减后库存 = 0
B 再扣减后库存 = -1
```

必须使用原子操作或事务控制。

### 挑战三：重复请求与支付回调不可靠

用户可能反复点击，网络可能重试；支付平台的回调也可能重复、延迟甚至乱序。因此所有关键写操作都必须具备**幂等性**。

---

## 4. 高层架构

```text
用户客户端
   │
   ▼
CDN / 静态页面缓存 / WAF
   │
   ▼
负载均衡 + API 网关
   ├── 鉴权
   ├── 限流
   ├── 黑名单与风控
   ├── 防重放校验
   └── 路由
   │
   ▼
秒杀接入服务
   ├── 本地缓存
   ├── 活动校验
   ├── 用户限购校验
   ├── Redis 原子预扣库存
   └── 写入消息队列
   │
   ▼
消息队列
   │
   ▼
订单消费者 / 订单服务
   ├── 创建订单
   ├── 持久化库存预占记录
   ├── 发送延迟取消消息
   └── 写入订单事件
   │
   ├──────────────► 订单数据库
   │
   ▼
支付服务 ─────────► 第三方支付平台
   │
   ▼
支付回调处理 / 对账系统
   │
   ▼
库存确认、订单状态更新、通知服务
```

---

## 5. 秒杀请求链路设计

## 5.1 活动开始前：预热与静态化

活动开始前完成：

- 商品详情页静态化并发布到 CDN；
- 商品图片和脚本走对象存储/CDN；
- 活动元数据加载到本地缓存和 Redis；
- 预加载秒杀库存到 Redis；
- 预生成或动态签发秒杀令牌；
- 对热点商品进行缓存预热；
- 进行压测和容量预留。

目标是让活动前的商品浏览尽量不访问业务服务和数据库。

---

## 5.2 活动开始后：入口拦截

请求先经过 CDN、WAF、API 网关和秒杀接入服务。

### 第一层：CDN 与静态资源

- 商品详情、活动规则、倒计时、图片等均走 CDN；
- 客户端不直接获得真实下单接口，降低被脚本直接攻击的概率；
- 页面可在接近开始时间再请求动态令牌。

### 第二层：网关限流与风控

网关按多个维度限流：

- IP；
- 用户 ID；
- 设备 ID；
- 商品 ID；
- API 路径；
- 全局 QPS。

常用策略：

- 令牌桶：允许短时突发；
- 漏桶：平滑输出；
- 滑动窗口：限制时间窗口内请求数。

对于可疑行为：

- 验证码；
- 滑块验证；
- 增加等待时间；
- 降低请求优先级；
- 直接拒绝。

### 第三层：资格和限购校验

抢购接口需要校验：

- 活动是否开始、是否结束；
- 商品是否有效；
- 用户是否登录；
- 秒杀令牌是否有效；
- 用户是否已经成功抢购；
- 用户是否处于黑名单；
- 当前系统是否处于过载降级状态。

---

## 5.3 Redis 原子预扣库存

Redis 非常适合处理高并发库存预扣，因为其单线程执行 Lua 脚本时能保证脚本内操作的原子性。

Redis 中可维护：

```text
seckill:stock:{campaignId}        -> 剩余库存
seckill:bought:{campaignId}       -> 已购买用户集合
seckill:request:{requestId}       -> 请求处理状态
```

伪逻辑如下：

```text
原子执行：
1. 判断剩余库存是否大于 0；
2. 判断当前用户是否已抢购；
3. 若库存充足且用户未抢购：
   - 扣减 Redis 库存；
   - 写入用户已抢购标记；
   - 返回成功；
4. 否则返回“售罄”或“重复购买”。
```

这一步的结果不是最终订单成功，而是：

> 用户获得了“库存预占资格”。

随后系统将下单任务写入消息队列。

---

## 5.4 通过消息队列削峰

秒杀接入服务不应同步执行完整的订单创建流程，因为数据库和订单服务无法直接承受数百万 QPS。

流程变为：

```text
用户请求
  → Redis 原子预扣库存
  → 投递“创建秒杀订单”消息
  → 立即返回“请求已受理，请查询结果”
```

订单消费者以数据库可承受的速率消费消息：

```text
消息队列
  → 创建订单
  → 写订单明细
  → 写库存预占记录
  → 发出订单创建成功事件
```

这样入口流量被削峰，用户体验由“立即得到最终结果”变成“排队后查询结果”。

这是一种合理取舍：在极端高并发下，**系统稳定性比同步响应更重要**。

---

## 6. 库存与订单的一致性设计

这里是秒杀系统的核心。

## 6.1 两阶段库存概念

将库存拆分为：

```text
总库存 = 可售库存 + 锁定库存 + 已售库存
```

状态变化为：

```text
初始：
可售库存 = 100,000
锁定库存 = 0
已售库存 = 0

用户抢到资格：
可售库存 - 1
锁定库存 + 1

支付成功：
锁定库存 - 1
已售库存 + 1

支付超时或取消：
锁定库存 - 1
可售库存 + 1
```

这样能清晰表达“用户抢到名额但尚未支付”的业务状态。

---

## 6.2 订单状态机

订单不应只使用模糊的布尔字段，而应定义明确状态机。

```text
PENDING_CREATE      待创建
PENDING_PAYMENT     待支付
PAID                已支付
CANCELLED           已取消
PAYMENT_FAILED      支付失败
REFUNDING           退款中
REFUNDED            已退款
```

典型合法迁移：

```text
PENDING_CREATE → PENDING_PAYMENT
PENDING_PAYMENT → PAID
PENDING_PAYMENT → CANCELLED
PAID → REFUNDING
REFUNDING → REFUNDED
```

非法状态迁移必须拒绝。例如已支付订单不能被“超时取消任务”直接取消。

---

## 6.3 幂等性设计

以下请求都可能重复：

- 用户提交秒杀请求；
- 消息队列投递；
- 订单消息消费；
- 支付回调；
- 超时取消任务；
- 库存释放任务。

因此需要：

| 场景 | 幂等方案 |
|---|---|
| 秒杀请求 | 请求 ID、用户限购标记、Redis 原子脚本 |
| 创建订单 | 唯一索引：`campaign_id + user_id` |
| MQ 消费 | 消息唯一 ID / 消费记录表 / 业务唯一键 |
| 支付回调 | 支付流水号唯一索引、订单状态条件更新 |
| 取消订单 | 仅在 `PENDING_PAYMENT` 状态下更新 |
| 释放库存 | 库存流水号唯一约束或去重标识 |

例如，创建订单时可设置唯一约束：

```sql
UNIQUE KEY uk_campaign_user (campaign_id, user_id)
```

即使消息被重复消费，也只能成功创建一笔订单。

---

## 6.4 支付成功与超时取消的竞争

可能发生以下竞态：

```text
14:59:59 用户完成支付
15:00:00 超时任务准备取消订单
15:00:01 支付回调到达系统
```

解决思路：

1. 使用订单状态的条件更新；
2. 每次状态变更都检查当前状态；
3. 支付平台以支付成功时间、交易流水为准；
4. 对边界订单进行主动查询和对账；
5. 若超时取消后支付成功，按照业务规则触发退款或人工处理。

示例条件更新：

```sql
UPDATE orders
SET status = 'CANCELLED'
WHERE order_id = ?
  AND status = 'PENDING_PAYMENT'
  AND expire_at < NOW();
```

若支付回调先将状态更新为 `PAID`，取消操作将影响零行，从而避免误取消已支付订单。

---

## 7. 数据存储设计

## 7.1 Redis

适合存储：

- 活动配置缓存；
- 热点商品信息；
- 秒杀库存；
- 用户限购集合；
- 短期幂等标记；
- 分布式限流计数器；
- 会话、令牌、验证码状态。

注意事项：

- Redis 不能是唯一事实来源；
- 要考虑持久化、主从复制、故障切换；
- 热点 Key 可能造成单分片瓶颈；
- 库存预扣与数据库最终库存要定期对账；
- 需要缓存过期、预热与降级策略。

## 7.2 关系型数据库

适合存储：

- 订单；
- 支付流水；
- 库存流水；
- 活动配置；
- 审计记录；
- 对账结果。

关系型数据库承担最终业务事实，需重点关注：

- 索引设计；
- 慢查询；
- 连接池；
- 主从复制延迟；
- 分库分表；
- 备份恢复；
- 数据归档。

### 分库分表的可能策略

当订单量很大时，可按用户 ID 或订单 ID 分片：

```text
order_db_{hash(user_id) % N}
order_table_{hash(order_id) % M}
```

取舍：

- 按用户 ID 分片便于查询“我的订单”；
- 按订单 ID 分片便于均匀写入；
- 跨分片统计会变复杂，通常转入数据仓库或分析系统处理。

---

## 8. 消息队列可靠性设计

消息队列用于削峰、异步解耦和最终一致性，但需要处理四类问题。

### 1. 消息丢失

措施：

- 生产者确认机制；
- 消息持久化；
- 多副本；
- 消费成功后再提交位点；
- 失败消息进入重试队列或死信队列；
- 使用 Outbox 模式保证“写库和发消息”的一致性。

### 2. 重复消费

措施：

- 消费者幂等；
- 消息唯一 ID；
- 业务唯一索引；
- 消费记录表。

### 3. 消息乱序

若某些场景要求顺序，例如同一订单的“创建、支付、取消”事件，则：

- 按订单 ID 路由到固定分区；
- 消费者按分区顺序消费；
- 仍需用状态机防止乱序事件造成错误更新。

### 4. 消息积压

措施：

- 监控消费延迟；
- 扩容消费者；
- 分区扩展；
- 将非关键任务隔离到独立 Topic；
- 设置降级策略；
- 控制生产速率。

---

## 9. 高可用与容灾设计

系统中不同组件的高可用策略不同。

| 组件 | 常见策略 |
|---|---|
| API 服务 | 无状态部署、多实例、负载均衡、自动扩缩容 |
| Redis | 主从复制、哨兵或集群、持久化、跨可用区部署 |
| 数据库 | 主从复制、自动故障切换、备份、恢复演练 |
| 消息队列 | 多副本、分区、副本同步、消费者重平衡 |
| 静态资源 | CDN、多地域节点、对象存储 |
| 配置中心 | 多副本、版本控制、灰度发布、回滚 |

需要根据业务目标选择容灾等级：

- **单机房高可用**：应对单机、单节点故障；
- **同城双活**：应对机房级故障；
- **异地多活**：应对城市级灾难，但成本和数据一致性复杂度显著增加。

对于秒杀业务，常见务实方案是：

- 静态内容多地域 CDN；
- 服务多可用区部署；
- 核心数据库主备；
- 活动期间必要时限制地域或进行流量调度；
- 出现极端故障时宁可暂停抢购，也不能错误扣款或超卖。

---

## 10. 安全与风控

秒杀系统本质上是对抗自动化脚本和恶意流量的系统。

应考虑：

- 身份认证：登录态、Token 校验；
- 授权：用户、运营人员、管理员权限隔离；
- 接口签名：防篡改、防重放；
- 限流：按 IP、账号、设备、商品等维度；
- 验证码：区分真人与机器；
- 设备指纹与行为分析；
- 黑名单和风险评分；
- 防 SQL 注入、XSS、CSRF、SSRF；
- 敏感信息加密与脱敏；
- 支付回调签名校验；
- 审计日志；
- 密钥轮换与最小权限原则。

重要原则：

> 客户端传来的库存、价格、优惠、订单金额都不可信。  
> 服务端必须基于自身的商品、活动和价格规则重新计算。

---

# 三、系统设计时必须考虑的问题清单

下面是一份实用检查清单。

## 1. 需求与边界

- 用户是谁？
- 核心使用场景是什么？
- 什么是不做的？
- 成功标准是什么？
- 哪些功能最重要？
- 哪些功能可以异步或延后实现？

## 2. 流量与容量

- 日请求量、峰值 QPS、读写比例？
- 数据量、增长率、保存周期？
- 单条数据大小和带宽需求？
- 是否有明显热点？
- 是否存在突发流量？

## 3. 数据与一致性

- 哪些数据是系统事实来源？
- 哪些操作必须原子？
- 强一致与最终一致的边界在哪里？
- 如何避免重复写入？
- 如何处理数据冲突？
- 如何做数据对账与修复？

## 4. 性能与扩展性

- 缓存放在哪里？
- 缓存失效怎么办？
- 数据库是否需要读写分离？
- 是否需要分库分表？
- 如何解决热点 Key、热点分区和热点数据库行？
- 服务是否无状态，是否能水平扩容？

## 5. 可靠性

- 单点故障在哪里？
- 下游超时怎么办？
- 是否有熔断、降级、隔离和限流？
- 重试会不会加剧故障？
- 消息丢失、重复、乱序、积压如何处理？
- 数据库故障如何恢复？

## 6. 安全与合规

- 身份认证和权限控制如何设计？
- 如何保护个人信息和敏感数据？
- 如何记录审计日志？
- 如何防止恶意流量与接口滥用？
- 数据保存、删除、访问是否符合适用的法律与行业要求？

## 7. 可运维性

- 有哪些关键指标？
- 怎样设置告警？
- 如何定位一次请求的全链路问题？
- 如何灰度发布、回滚和紧急止损？
- 如何备份、恢复和演练？
- 是否有 Runbook（故障处置手册）？

## 8. 成本与团队能力

- 方案是否超过当前团队的维护能力？
- 是否引入过多中间件？
- 云服务、存储、带宽和计算成本是否可接受？
- 是否可以先使用简单架构，再按规模演进？

---

# 四、做好系统设计需要具备的知识体系

系统设计不是单一技术栈知识，而是多层次知识的组合。

## 1. 计算机基础

这是理解系统行为的底层能力。

- 数据结构与算法；
- 操作系统：进程、线程、内存、文件系统、I/O；
- 计算机网络：TCP/IP、HTTP/HTTPS、DNS、TLS、负载均衡；
- 并发编程：锁、原子操作、线程池、死锁、竞态条件；
- 编码与序列化：JSON、Protobuf、压缩、字符编码。

---

## 2. 数据库与数据建模

需要掌握：

- 关系型数据库原理；
- SQL、索引、执行计划、事务；
- ACID、隔离级别、MVCC；
- 乐观锁、悲观锁；
- 主从复制、读写分离；
- 分库分表与数据迁移；
- NoSQL 的适用场景；
- 数据仓库、OLTP 与 OLAP 的区别；
- 数据备份、恢复、归档和对账。

特别重要的是理解：

> 数据库不是“存数据的黑盒”，而是系统正确性、性能和成本的核心约束之一。

---

## 3. 分布式系统

这是中大型系统设计的核心。

应理解：

- CAP 思想与网络分区；
- 一致性模型；
- 共识与领导者选举的基本概念；
- 分布式锁；
- 分布式 ID；
- 幂等性；
- 去重；
- 消息队列语义；
- 最终一致性；
- Saga、Outbox、补偿事务；
- 服务发现、配置中心；
- 时钟偏差与时间语义；
- 多副本、容灾与多活。

不一定要手写共识算法，但必须知道其解决的问题和代价。

---

## 4. 缓存、消息与异步架构

需要掌握：

### 缓存

- 缓存穿透、击穿、雪崩；
- Cache Aside（旁路缓存）模式；
- 缓存更新策略；
- TTL、预热、淘汰策略；
- 缓存一致性；
- 热点 Key；
- 分布式缓存集群。

### 消息队列

- 发布订阅、队列、消费者组；
- 至少一次、至多一次、恰好一次等语义的实际含义；
- 顺序、重复、延迟、积压；
- 重试队列、死信队列；
- 消费幂等与消息可追溯性。

---

## 5. 云原生与基础设施

现代系统设计通常离不开：

- Linux 基础；
- 容器与 Docker；
- Kubernetes 基础；
- 服务部署与自动扩缩容；
- 负载均衡；
- CI/CD；
- 基础设施即代码；
- 云数据库、对象存储、CDN；
- 多可用区部署；
- 配置管理与密钥管理。

重点不是“会背所有 Kubernetes 资源对象”，而是理解部署、扩缩容、网络、资源隔离和故障恢复。

---

## 6. 安全工程

至少需要理解：

- 认证与授权：Session、JWT、OAuth 等基本思路；
- RBAC / ABAC 权限模型；
- HTTPS、TLS、证书；
- 常见 Web 攻击：SQL 注入、XSS、CSRF、SSRF；
- DDoS 防护与限流；
- 数据加密、脱敏、密钥管理；
- 安全审计；
- 最小权限原则；
- 供应链安全与依赖漏洞管理。

---

## 7. 可观测性与 SRE

系统设计不只关注“上线”，还应关注“长期稳定运行”。

需要掌握：

- Metrics、Logs、Traces 三大支柱；
- SLI、SLO、SLA；
- 容量规划；
- 告警设计；
- 故障演练；
- 混沌工程的基本思想；
- 值班响应与事故复盘；
- RTO（恢复时间目标）与 RPO（恢复点目标）。

---

## 8. 领域知识与业务理解

技术方案必须服务于业务。

例如：

- 金融系统重视资金正确性、审计和监管；
- 电商系统重视库存、订单、支付、促销；
- 社交系统重视关系链、内容分发和隐私；
- 物流系统重视状态流转、地理信息和调度；
- 医疗系统重视数据隐私、准确性和合规；
- AI 平台重视数据管道、模型服务、GPU 资源和成本。

不了解业务规则，很容易设计出“技术上正确、业务上不可用”的系统。

---

# 五、学习与实践路径

如果你希望系统性提升系统设计能力，可以按以下路径推进。

## 阶段一：打牢基础

- 熟悉一门后端语言；
- 掌握 HTTP、TCP、DNS、Linux；
- 熟练使用关系型数据库；
- 理解索引、事务、隔离级别与 SQL 优化；
- 实现一个带用户、商品、订单的基础 Web 服务。

## 阶段二：掌握常用组件

- 在项目中接入 Redis；
- 使用消息队列实现异步任务；
- 学习对象存储、CDN 和搜索引擎；
- 学习限流、熔断、重试、幂等；
- 为服务增加日志、指标、链路追踪。

## 阶段三：解决分布式问题

- 实现订单创建与库存扣减；
- 处理重复请求和重复消息；
- 设计支付回调幂等；
- 实现延迟取消订单；
- 完成库存对账与补偿；
- 体验数据库读写分离、分片和迁移问题。

## 阶段四：进行真实设计练习

可以反复练习以下题目：

1. URL 短链系统；
2. 文件存储与分享系统；
3. 聊天系统；
4. 消息推送系统；
5. 新闻 Feed 流系统；
6. 分布式任务调度系统；
7. 在线支付系统；
8. 外卖订单与配送系统；
9. 网约车匹配系统；
10. 秒杀系统；
11. 日志采集与分析平台；
12. 视频上传、转码与播放平台。

每次练习建议固定使用同一模板：

```text
1. 需求澄清
2. 非功能要求
3. 容量估算
4. API 设计
5. 数据模型
6. 高层架构
7. 关键链路
8. 一致性与事务
9. 性能和扩展
10. 容灾、降级与安全
11. 监控运维
12. 方案取舍与未来演进
```

---

# 六、总结

系统设计能力本质上是以下能力的综合：

- **抽象能力**：从复杂业务中提取核心问题；
- **量化能力**：用流量、容量和延迟指导架构；
- **建模能力**：正确设计数据、状态和边界；
- **分布式思维**：理解失败、延迟、重复与不一致是常态；
- **工程权衡能力**：在正确性、性能、成本、复杂度之间取舍；
- **风险意识**：主动设计故障、攻击和异常处理路径；
- **运维意识**：确保系统可观测、可发布、可恢复；
- **业务理解能力**：用技术真正解决业务问题。

最重要的一点是：**不要追求“最复杂”或“最前沿”的架构，而要设计在当前约束下最合适、可验证、可演进的系统。**

